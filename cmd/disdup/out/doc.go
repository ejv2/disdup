// Package out implements custom or higher level outputs than that which are
// implemented by Disdup directly. These are designed for use by a user through
// shell scripting or the disdup program directly, rather than being imported
// by another package.
package out
